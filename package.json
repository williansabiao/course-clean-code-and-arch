{
  "name": "clean-code-arch",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "tsc:init": "tsc --init",
    "build": "tsc",
    "test": "tsc && jest dist/",
    "test:watch": "npm run build && concurrently --kill-others \"tsc --watch\" \"jest dist/ --watchAll\""
  },
  "dependencies": {
    "typescript": "^4.2.4"
  },
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "concurrently": "^6.2.0",
    "jest": "^26.6.3"
  }
}
